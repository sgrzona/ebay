<%= form_for @profile, url: user_profile_path(current_user) do |p| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



     <div class="form-group">
       <%= p.label :first_name %>
       <%= p.text_field :first_name, :class => "form-control" %>

      <div class="form-group">
       <%= p.label :last_name %>
       <%= p.text_field :last_name, :class => "form-control" %>
     </div>



      <div class="form-group">
        <%= p.label :blurb %>
        <%= p.text_area :blurb, :class => "form-control" %>
         <%= p.hidden_field :user_id, :value => current_user.id %>

      </div>


     <div class="actions">
       <%= p.submit class: "btn-btn-primary"%>
    </div>

<% end %>
