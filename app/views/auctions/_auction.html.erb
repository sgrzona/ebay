
<table>
<thead>
  <tr>
      <%= link_to auction do %>
      <%= image_tag auction.image.url(:thumb) %>
    <% end %>
  <td><%= auction.title %></td></center>
  <td><%= auction.description %></td>
  <td><%= auction.expires_at_date %></td>
  <td><%= auction.highest_auction_bid %></td>

  <% if auction.user == current_user %>
    <td><%= link_to 'Edit', edit_auction_path(auction) %></td>
    <td><%= link_to 'Destroy', auction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <% end %>
</tr>
</thead>
</table>
