<table>
<thead>
  <tr>
    <td><%= link_to auction do %>
      <%= image_tag auction.image.url(:thumb) %></td>
    <% end %>
  <td><%= auction.title %></td></center>
  <td><%= auction.description %></td>
  <td><%= auction.expires_at.present? ? auction.expires_at.strftime("%m/%d/%Y %l:%M %P") : "Not set" %></td>
  <td><%= auction.highest_auction_bid %></td>

  
</tr>
</thead>
</table>
